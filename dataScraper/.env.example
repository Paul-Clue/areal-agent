# Copy to .env and fill in. Never commit .env.
# Admin / bulk ingest (direct port on DO, e.g. 25060):
# DATABASE_URL=postgresql://doadmin:PASSWORD@HOST:25060/DATABASE?sslmode=require
#
# Application runtime should use app_user + PgBouncer (e.g. port 25061) per databasePlanv2.md Step 7.

DATABASE_URL=
